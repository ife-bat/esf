# Development records and information

This folder contains documents that are meant to help developers, as well as
documents that plan and track the development.

They are working records kept in the order they were written, so they describe
the state of the code at their date rather than the state today — including
paths and components that have since moved or been dropped. Read them for the
reasoning behind the current design; read `docs/` for what the code does now.